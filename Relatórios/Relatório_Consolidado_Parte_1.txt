========================================================================================
====================================== 03/09/2026 ======================================
========================================================================================

Hoje foi iniciado o desenvolvimento do projeto de Trabalho de Conclusão de Curso (TCC), cujo objetivo é realizar a classificação de tumores cerebrais em imagens de ressonância magnética utilizando técnicas de transferência de aprendizado (Transfer Learning).

O projeto realizará uma análise comparativa dos modelos utilizados, considerando aspectos como desempenho na classificação, eficiência computacional e explicabilidade dos resultados. Para organizar o desenvolvimento e facilitar o acompanhamento do projeto, o trabalho foi dividido em diferentes etapas.

1.1 - Configuração do ambiente de desenvolvimento

Inicialmente, foi definido o Visual Studio Code (VS Code) como ambiente de desenvolvimento do projeto. O ambiente será configurado para utilização da linguagem Python e das bibliotecas necessárias para processamento, análise e visualização dos dados.

Nesta etapa, serão instaladas inicialmente as seguintes bibliotecas:
 - Pillow: manipulação e processamento de imagens;
 - ImageHash: geração e comparação de hashes de imagens;
 - pandas: organização, manipulação e análise dos dados;
 - NetworkX: criação e análise de estruturas baseadas em grafos;
 - Matplotlib: geração de gráficos e visualização dos dados.

Essas bibliotecas serão utilizadas ao longo das próximas etapas do desenvolvimento, principalmente durante a organização e análise do conjunto de dados, processamento das imagens e visualização dos resultados.

1.1.1 - Criação do ambiente virtual

Foi criado um ambiente virtual Python (.venv) específico para o projeto, com o objetivo de isolar suas dependências.

Comando utilizado:
python -m venv .venv

1.1.2 - Instalação das dependências

Comando utilizado:
python -m pip install pillow imagehash pandas networkx matplotlib

Resultado:
Todas as bibliotecas e suas respectivas dependências foram instaladas com sucesso no ambiente virtual. Após a instalação, foi gerado o arquivo requirements.txt, contendo as versões das dependências utilizadas no projeto, permitindo a reprodução futura do ambiente de desenvolvimento.

Comando utilizado:
python -m pip freeze > requirements.txt

Status: CONCLUÍDO
[X] Criar ambiente virtual Python e instalar pillow, imagehash, pandas, networkx, matplotlib.

=========================================================================================
====================================== 04/09/2026 =======================================
=========================================================================================

Hoje iremos continuar desenvolvendo à Prova de Conceito (PoC) da etapa de auditoria da base. Começaremos o desenvolvimento utilizando apenas 100 imagens de uma única classe do conjunto de treino (refente a pasta 'testing' no BD), a classe escolhida foi: meningioma. Essa metadologia foi proposta a fim de realizar a validação do funcionamento básico do pipeline antes de aplicar na base completa.

1.2.1 - Localização das imagens
Primeiramente, será criado o script:
    poc_auditoria.py

O primeiro objetivo do script será:
    - acessar uma pasta de uma classe do conjunto de treino;
    - identificar os arquivos de imagem existentes;
    - selecionar apenas 100 imagens;
    - exibir no terminal a quantidade de imagens encontradas;
    - exibir alguns caminhos para conferir se a leitura da pasta está correta.
=========================================================================================
====================================== 07/09/2026 ========================================
=========================================================================================

Reorganização do código da PoC

Depois da primeira versão funcionando, reorganizamos o código em funções para separar a seleção das imagens, o cálculo de SHA-256, a extração de metadados, a montagem dos registros e a conferência do CSV.

Também substituímos o campo dimensions por width e height, deixando largura e altura em colunas separadas. Com isso, o manifesto da PoC passou de 14 para 15 colunas.

Adicionamos uma verificação para impedir que a PoC continue com menos de 100 imagens e outra para interromper a execução caso os hashes não sejam preservados após a exportação.

Os caminhos passaram a ser definidos a partir da localização do script. Isso funcionou mesmo depois de renomear a pasta principal do projeto para Projeto do TCC.

Executamos novamente a versão reorganizada e obtivemos 100 registros válidos, 15 colunas, nenhum hash ausente e “Hashes preservados: True”.

Status da Etapa 0: CONCLUÍDA — o fluxo da PoC foi validado nas 100 imagens, incluindo exportação, reabertura e o teste separado com um arquivo não reconhecido como imagem.

Etapa 1 - Auditoria da Base

Com a PoC funcionando, começamos o script auditoria.py dentro de Desenvolvimento/Etapa 1 - Auditoria da Base.

Primeiro, conferimos a estrutura do dataset. Foram encontradas as pastas Training e Testing, ambas contendo as classes glioma, meningioma, notumor e pituitary.

Criamos um mapeamento para registrar Training como train e Testing como test no manifesto. A auditoria trabalha apenas com esses dois conjuntos, sem criar val.

Contagem e coleta dos arquivos

Reutilizamos o filtro de extensões .jpg, .jpeg e .png para contar os arquivos de cada classe.

Na primeira tentativa, a contagem mostrou apenas 400 arquivos da classe pituitary do teste. O problema estava na indentação: o bloco de contagem havia ficado fora do laço correspondente. Depois do ajuste, obtivemos:

- Train: 1.400 arquivos por classe, totalizando 5.600;
- Test: 400 arquivos por classe, totalizando 1.600;
- Total da base: 7.200 arquivos.

Depois, guardamos cada caminho junto com seu split e sua classe em uma lista chamada image_entries. Conferimos a primeira e a última entrada para verificar se a origem dos arquivos estava sendo registrada corretamente.

Adaptação do processamento

Copiamos as funções da PoC e adaptamos build_record() para receber o caminho, o split e a classe de cada imagem. O split deixou de ser fixo em train, permitindo registrar corretamente as imagens de Testing.

Durante essa adaptação, corrigimos uma importação de Image que estava vindo de tkinter, quando deveria vir de PIL. Também adicionamos a importação de hashlib.

Antes de executar na base completa, testamos uma imagem de test/pituitary. O registro apresentou o split e a classe corretos, metadados preenchidos, os três hashes e is_valid=True.

Processamento da base completa

Depois do teste individual, executamos o processamento nas 7.200 imagens. Adicionamos mensagens de progresso a cada 500 arquivos e ao final da execução. Essas mensagens servem apenas para acompanhamento, sem salvar checkpoints.

O resultado foi:

- 7.200 registros gerados;
- 7.200 registros com is_valid=True;
- Nenhuma falha capturada durante o processamento.

Esse resultado indica que a leitura e os cálculos implementados foram concluídos sem erro. Ele não confirma a ausência de duplicatas, problemas de rótulo ou outros problemas que dependam de análise posterior.

Exportação e conferência

Organizamos os registros com pandas e geramos dois arquivos na pasta resultados:

- manifesto_base_dados.csv: todos os registros da auditoria;
- relatorio_erros.csv: apenas os registros com falha.

Como nenhuma falha foi registrada, relatorio_erros.csv ficou apenas com os cabeçalhos.

Reabrimos o manifesto com os hashes definidos como texto e conferimos a quantidade de linhas, as colunas e os valores de SHA-256, pHash e dHash. Também verificamos a distribuição por split e classe.

Nessa versão, o manifesto apresentou 7.200 linhas e 15 colunas, nenhum hash ausente e “Hashes preservados: True”. As contagens permaneceram em 1.400 imagens por classe no treino e 400 por classe no teste.

Análise dos modos de cor e das dimensões

Além de verificar a leitura, examinamos os modos de cor e a quantidade de canais. Encontramos:

- L: 3.067 imagens, com uma banda de escala de cinza;
- RGB: 4.129 imagens, com três canais;
- RGBA: 3 imagens, com quatro canais;
- P: 1 imagem, com uma banda de índices de uma paleta.

O modo P não significa necessariamente escala de cinza, pois seus valores apontam para uma paleta de cores.

Também encontramos diferentes dimensões na base. A mais frequente foi 512 × 512, presente em 5.014 imagens. Outras dimensões comuns foram 225 × 225, com 338 imagens, e 630 × 630, com 90 imagens.

Nenhum registro apresentou largura ou altura ausente, igual a zero ou negativa. As diferenças de dimensões e modos de cor foram documentadas, sem alterar ou excluir imagens.

Investigação dos arquivos em P e RGBA

Separamos os quatro arquivos em P ou RGBA no relatório revisao_modos_cor.csv. Todos estavam na classe notumor do treino:

- Tr-no_1200.jpg: RGBA;
- Tr-no_22.jpg: RGBA;
- Tr-no_572.jpg: RGBA;
- Tr-no_625.jpg: P.

Como todos tinham extensão .jpg, verificamos o formato identificado pelo Pillow. Os quatro foram reconhecidos como PNG.

Isso mostrou uma divergência entre o nome e o conteúdo dos arquivos. Eles puderam ser lidos normalmente, portanto essa divergência não foi tratada como motivo para exclusão.

Conferência do formato na base inteira

Adicionamos o campo image_format ao manifesto para verificar se havia outros casos além dos quatro já encontrados.

A análise das 7.200 imagens mostrou:

- 7.196 arquivos com extensão .jpg e formato JPEG;
- 4 arquivos com extensão .jpg e formato PNG.

As quatro divergências foram salvas em divergencias_formato.csv. Os arquivos originais foram mantidos, preservando os bytes e os hashes calculados.

Inclusão do caminho relativo

Por último, acrescentamos o campo relative_path, mantendo também o caminho absoluto em file_path.

O caminho relativo usa a raiz do dataset como referência e ficou no formato:

Training/glioma/Tr-gl_1.jpg

Isso facilita localizar os arquivos caso o projeto seja movido para outra pasta ou computador.

Com width e height separados, image_format e relative_path, o manifesto final passou a ter 17 colunas.

Conferência final da auditoria

A última execução apresentou:

- 7.200 registros e 17 colunas;
- 7.200 registros válidos segundo o processamento implementado;
- Nenhuma falha de leitura capturada;
- Nenhum registro sem dimensões válidas;
- Nenhum SHA-256, pHash ou dHash ausente;
- Hashes preservados após salvar e reabrir o CSV;
- Distribuição por split e classe mantida;
- Quatro divergências entre extensão e formato documentadas.

Foram gerados os seguintes arquivos na pasta resultados da Etapa 1:

- manifesto_base_dados.csv;
- relatorio_erros.csv;
- revisao_modos_cor.csv;
- divergencias_formato.csv.

Nenhuma imagem foi removida, renomeada ou convertida durante a auditoria.

Próximos passos

Começar a Etapa 2 - Deduplicacao e Sensibilidade utilizando o manifesto já exportado, sem recalcular os hashes.

O primeiro passo será carregar o CSV no script deduplicacao_exata.py e agrupar arquivos com o mesmo SHA-256. Depois, verificar duplicatas dentro de cada split, entre train e test e entre classes diferentes.

Na sequência, serão feitas as comparações perceptuais com pHash e dHash, a análise dos limiares T={0,3,5,8,10} e o agrupamento em componentes conexos.

As decisões de exclusão ficarão para a curadoria visual, seguindo o protocolo human-in-the-loop. Nenhuma imagem será excluída automaticamente.

Status da Etapa 1: processamento e conferências descritos acima concluídos, com manifesto e relatórios gerados. A execução da deduplicação da Etapa 2 ainda não foi confirmada.

Etapa 2 - Deduplicacao e Sensibilidade

Hoje começamos a análise de duplicatas utilizando o manifesto gerado na auditoria. Os hashes já estavam calculados, então não foi necessário abrir novamente as 7.200 imagens para recalculá-los.

Leitura do manifesto

Carregamos o manifesto_base_dados.csv, preservando SHA-256, pHash e dHash como texto para evitar alterações nos valores, como a perda de zeros à esquerda.

A leitura apresentou 7.200 registros e nenhum SHA-256 ausente.

Deduplicação exata com SHA-256

Primeiro, agrupamos os arquivos que possuem o mesmo SHA-256. Encontramos:

- 153 grupos com hash repetido;
- 340 arquivos envolvidos nesses grupos;
- 187 cópias excedentes em relação a manter um arquivo por grupo.

Essa quantidade de cópias excedentes é apenas uma contagem. Nenhum arquivo foi escolhido para exclusão ou removido.

Depois, verificamos se os grupos atravessavam os conjuntos de treino e teste ou apresentavam classes diferentes. O resultado foi:

- Nenhum grupo presente simultaneamente em train e test;
- Nenhum grupo com classes diferentes.

Portanto, todas as duplicatas exatas encontradas ficaram dentro de um único split e de uma única classe por grupo.

Salvamos os resultados em:

- duplicatas_exatas.csv: detalhamento dos 340 arquivos;
- resumo_grupos_exatos.csv: resumo dos 153 grupos.

Comparação perceptual com pHash e dHash

Na sequência, começamos a comparação dos hashes perceptuais, que permite encontrar candidatos visualmente semelhantes mesmo quando os arquivos não possuem os mesmos bytes.

Primeiro, comparamos duas imagens para conferir o funcionamento da distância de Hamming. O resultado foi 22 bits diferentes no pHash e 20 no dHash. Esse par ficou fora de todos os limiares planejados.

Depois, transformamos os hashes em duas matrizes, uma para pHash e outra para dHash, cada uma com 7.200 linhas e 64 bits por imagem.

Utilizamos pdist para comparar os pares sem repetição e sem comparar uma imagem com ela mesma. Como a métrica hamming retorna a proporção de bits diferentes, multiplicamos o resultado por 64 para trabalhar com a quantidade de bits.

Foram comparados 25.916.400 pares por hash.

Análise de sensibilidade

Testamos os limiares T={0,3,5,8,10}, avaliando pHash e dHash separadamente. Para cada limiar, contamos os pares detectados, os pares entre train e test e os pares com classes diferentes.

Resultados do pHash:

| T | Pares detectados | Entre train/test | Classes diferentes | Classes diferentes (%) |
|---|---:|---:|---:|---:|
| 0 | 1.930 | 698 | 2 | 0,10% |
| 3 | 3.885 | 1.380 | 11 | 0,28% |
| 5 | 5.893 | 2.027 | 47 | 0,80% |
| 8 | 14.853 | 4.740 | 1.992 | 13,41% |
| 10 | 31.956 | 10.024 | 9.323 | 29,17% |

Resultados do dHash:

| T | Pares detectados | Entre train/test | Classes diferentes | Classes diferentes (%) |
|---|---:|---:|---:|---:|
| 0 | 2.776 | 1.031 | 6 | 0,22% |
| 3 | 7.947 | 2.757 | 208 | 2,62% |
| 5 | 17.874 | 5.688 | 2.178 | 12,19% |
| 8 | 93.549 | 29.954 | 28.798 | 30,78% |
| 10 | 257.956 | 82.519 | 104.126 | 40,37% |

O dHash sinalizou mais pares em todos os limiares. Também observamos um crescimento forte dos pares entre classes diferentes nos limiares maiores.

Esses percentuais não representam uma taxa comprovada de falsos positivos. Os casos podem envolver semelhança visual sem duplicação, limitações dos hashes ou problemas de rótulo, e precisam ser examinados.

Mesmo em T=0, encontramos candidatos entre treino e teste. Isso não contradiz o resultado do SHA-256: arquivos diferentes podem produzir o mesmo hash perceptual.

Os resultados foram organizados na tabela de sensibilidade, com exportação prevista no código para analise_sensibilidade.csv. O limiar final ainda não foi escolhido.

Seleção dos pares para revisão

Selecionamos os pares com distância zero em pelo menos um dos hashes perceptuais e que apresentavam diferença de split ou de classe.

Cada par foi incluído uma única vez, mesmo quando pHash e dHash eram iguais simultaneamente. Guardamos os caminhos relativos, splits, classes e as duas distâncias.

O resultado foi:

- 1.236 pares únicos selecionados;
- 1.233 pares entre treino e teste;
- 6 pares com classes diferentes;
- 3 pares com ambas as condições.

As categorias se sobrepõem, então suas contagens não devem ser somadas como se fossem independentes.

O relatório foi salvo como pares_t0_para_revisao.csv, na pasta resultados da Etapa 2.

Esses números representam pares, não a quantidade de imagens únicas nem de grupos. Uma mesma imagem pode participar de vários pares.

Ponto em que paramos

O próximo passo será separar os seis pares com classes diferentes e conferir seus caminhos e distâncias. O trecho para gerar pares_t0_classes_diferentes.csv foi proposto, mas sua execução ainda não foi confirmada.

Depois, vamos montar uma visualização lado a lado para inspecionar esses casos. Nenhuma inspeção visual foi concluída nesta sessão.

Pendências

- Separar e inspecionar os seis pares de T=0 com classes diferentes;
- Examinar os candidatos entre treino e teste;
- Documentar o critério de escolha do limiar final, distinguindo a análise exploratória já realizada de uma regra definida previamente;
- Implementar o agrupamento em componentes conexos;
- Registrar as decisões de curadoria seguindo o protocolo human-in-the-loop.

Nenhuma imagem foi excluída, renomeada, convertida ou reclassificada. Nos casos de rótulos conflitantes confirmados, será mantida a regra de documentar e excluir por ambiguidade, sem atribuir um novo diagnóstico por conta própria.

Status: EM DESENVOLVIMENTO — levantamento das duplicatas exatas e análise de sensibilidade realizados. Lista inicial de candidatos perceptuais exportada. Inspeção visual, escolha do limiar final, componentes conexos e decisões de curadoria ainda pendentes.